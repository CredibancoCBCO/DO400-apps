pipeline{
    agent any

    stages{
        stage('Deploy') {
            steps {
                sh '''
                    oc project gyekiw-deploy-strategies
                    oc start-build greeting-service --follow --wait
                '''
            }
        }
    }
}

